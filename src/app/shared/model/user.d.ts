export declare class User {
    name: string;
    organisation: string;
    organisationFullName: string;
    roles: string[];
    organisationTypes: string[];
    language: string;
    constructor(name: string, organisation: string, organisationFullName: string, roles: string[], organisationTypes: string[], language: string);
}
